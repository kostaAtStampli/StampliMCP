{
  "permissions": {
    "allow": [
      "Bash(git commit -m \"feat(ValidationCheckerTool): add fuzzy operation name detection\n\n- Inject FuzzyMatchingService\n- Add fuzzy matching in FindFlowForOperationFallback (exact + fuzzy)\n- Add IsOperationType helper with fuzzy fallback (threshold 0.60)\n- Apply fuzzy matching to all operation type checks (vendor, payment, po, item, pagination)\n- Benchmark logging for flow fallback detection\n\nUse case: ''exprt vendro'' → vendor_export_flow (typo corrected)\nPerformance: ~1-2ms for operation name fuzzy matching\")"
    ],
    "deny": [],
    "ask": []
  }
}
